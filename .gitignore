*.swp
package-lock.json
__pycache__
.pytest_cache
.venv
*.egg-info

# CDK asset staging directory
.cdk.staging
cdk.out

# Per-user CDK context cache (account/region/zone lookups). Each deployer
# resolves this against their own AWS account on first synth.
cdk.context.json

secrets/**
