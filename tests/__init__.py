# Required by `python -m unittest discover -s tests -t <repo_root>`: when the
# start directory differs from the top-level directory, unittest refuses to
# load the start directory as a namespace package and errors with
# "Start directory is not importable". Do not delete.
